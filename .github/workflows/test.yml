name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install
        run: pip install -e .
      - name: Unit tests
        run: python -m unittest discover -s tests -v
      - name: Fixture CLIs
        run: |
          python -m cel
          python -m cel.ingest replay data/fixtures/sample.jsonl
          python -m cel.research --path data/fixtures/sample.jsonl
          python -m cel.execution --path data/fixtures/sample.jsonl
